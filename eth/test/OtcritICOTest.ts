console.log(`Web3 version: ${web3.version.api}`);

contract('OTCPreICO', function(accounts: string[]) {
  it('should be able to set limits', async () => {
    assert.equal(10, 10);
  });
});

export = {};